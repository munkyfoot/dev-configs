# --- Helper functions: virtualenv + project utilities ---
#
# This file is sourced by `.zshrc` to provide small, focused helpers.
# Logic is shared across shells in `shared/aliases.sh`.

# Get the absolute directory where this script is located
typeset -g _ALIASES_SCRIPT_DIR="${_ALIASES_SCRIPT_DIR:-${${(%):-%x}:A:h}}"

# Shared helpers
typeset -g DC_SHARED_DIR="${DC_SHARED_DIR:-${_ALIASES_SCRIPT_DIR}/../shared}"
source "${DC_SHARED_DIR}/aliases.sh"
# --- end ---
